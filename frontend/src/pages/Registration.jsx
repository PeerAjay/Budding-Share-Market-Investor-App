function Registration({ onRegister }) {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Registration Page</h1>
      <p>Placeholder page for now.</p>
      <button onClick={onRegister}>Call Register Function</button>
    </div>
  )
}

export default Registration